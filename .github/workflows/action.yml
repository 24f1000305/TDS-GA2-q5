name: Email Action Workflow

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: 24f1000305@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Final step
        run: echo "Workflow completed"
